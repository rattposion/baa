import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const Dashboard: React.FC = () => {
  return (
    <Box>
      <Heading mb={6}>Dashboard</Heading>
      {/* Conteúdo do dashboard será implementado posteriormente */}
    </Box>
  );
};

export default Dashboard;